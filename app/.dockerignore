# Keep the build context lean and keep secrets out of the image.
.git
.gitignore
*.md

# Never bake secrets or local config into the Lambda image.
config/.env
.env

# Local logs / throwaway dirs
logs
_logs
archives

# macOS noise
.DS_Store
